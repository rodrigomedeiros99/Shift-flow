# styles/

Reserved for additional global styles. The design-system tokens (colors,
typography, status indicators) are defined in `src/app/globals.css` via the
TailwindCSS v4 `@theme` directive — that file is the single source of truth for
color (Engineering Standards §5).
