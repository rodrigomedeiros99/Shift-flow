# features/

Feature-based business logic (Engineering Standards §1 & §4). Each feature owns
its domain logic, queries, and feature-specific components — UI components only
render results, never compute business rules.

Planned modules (by phase): `planning`, `rotation`, `history`, `templates`,
`assignments`, `tv`.
